# .env.local - local development environment variables
# IMPORTANT: Fill the values below for your local setup. Do NOT commit this file to source control.
# Add any provider/API keys your app requires. Use NEXT_PUBLIC_ prefix for values that must be available in browser.

# Base URL for any local API the frontend should call
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Example API keys / secrets (leave blank and populate with real values)
# OPENAI (server-side only - do not expose client-side)
OPENAI_API_KEY=

# Back-end API URL used by frontend utilities (matches utils/emotionDetection.ts)
# Quickstart notes the Python backend runs on http://localhost:5000
NEXT_PUBLIC_API_URL=http://localhost:5000

# Example public keys (safe to expose to client only if truly public)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Any other environment variables used by the project can be added below
# e.g. NEXTAUTH_URL=http://localhost:3000
#       NEXT_PUBLIC_VOICE_API_KEY=
